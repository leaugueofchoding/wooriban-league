# M10.6 v2b. 원소반응 플래시 렌더링 수정

## 문제

원소반응 사운드는 재생되지만 중앙 플래시 이펙트가 보이지 않았다.

## 수정

- `<ReactionFlashOverlay>` 실제 렌더링 블록을 보장
- 중앙 플래시 크기, z-index, 글자 크기, 링 크기 강화
- debug log 삽입은 선택 처리로 변경하여 패치 실패를 막음

## 확인

- 감전/증발/과부하 등 반응 발생 시 중앙에 큰 반응명 배지가 떠야 한다.
- 사운드만 나고 배지가 안 뜨면 JSX 렌더링 블록이 들어갔는지 확인한다.
